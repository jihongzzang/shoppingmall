import {
  gray, blue, red, green, blackA,
} from '@radix-ui/colors';

const colors = {
  white: '#FFFFFF',
  black: '#000000',

  ...blackA,
  ...gray,
  ...blue,
  ...red,
  ...green,
};

export default colors;
