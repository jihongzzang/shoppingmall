import styled from 'styled-components';

import {
  Label as RadixLabel,
  Root as RadixLabelRoot,
} from '@radix-ui/react-label';

export const LabelRoot = styled(RadixLabelRoot)``;

export const Label = styled(RadixLabel)``;
